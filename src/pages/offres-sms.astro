---
import BaseLayout from '../layouts/BaseLayout.astro';
import Footer from '../components/Footer.astro';
---

<BaseLayout title="Offre Spéciale Artisan | Express Artisan" description="Profitez de votre site web pro pour 10€/mois. Offre limitée suite à notre échange SMS.">
    <nav class="simple-nav">
        <div class="logo">EXPRESS<span>ARTISAN</span></div>
    </nav>

    <main class="sms-landing">
        <section class="sms-hero">
            <div class="badge-alert">OFFRE EXCLUSIVE PAR SMS</div>
            <h1>Votre site pro est prêt. <br><span>Il ne manque que vous.</span></h1>
            <p>Suite à notre échange, nous avons réservé un créneau pour lancer votre visibilité Google cette semaine.</p>
            
            <div class="offer-highlight">
                <div class="oh-item">
                    <span class="price">10€</span>
                    <span class="label">par mois</span>
                </div>
                <div class="oh-plus">+</div>
                <div class="oh-item">
                    <span class="bonus">GRATUIT</span>
                    <span class="label">Frais de création</span>
                </div>
            </div>

            <div class="cta-direct">
                <a href="https://wa.me/33616552811?text=Bonjour%20David,%20je%20viens%20du%20SMS,%20je%20souhaite%20lancer%20mon%20site." class="btn-whatsapp-large">
                    <i class="fab fa-whatsapp"></i> Confirmer sur WhatsApp
                </a>
                <p class="sub-cta">Réponse instantanée garantie</p>
            </div>
        </section>

        <section class="sms-benefits">
            <h2>Pourquoi maintenant ?</h2>
            <div class="benefit-row">
                <div class="b-icon">⚡</div>
                <div class="b-text">
                    <strong>En ligne sous 24h</strong>
                    <p>On s'occupe de tout. Vous ne perdez pas une minute de chantier.</p>
                </div>
            </div>
            <div class="benefit-row">
                <div class="b-icon">📍</div>
                <div class="b-text">
                    <strong>Fiche Google Booster</strong>
                    <p>On lie votre site à Google pour faire sonner votre téléphone.</p>
                </div>
            </div>
            <div class="benefit-row">
                <div class="b-icon">🎁</div>
                <div class="b-text">
                    <strong>100 Cartes offertes</strong>
                    <p>Inclus dans notre pack lancement pour cette semaine uniquement.</p>
                </div>
            </div>
        </section>

        <section class="sms-urgency">
            <div class="urgency-card">
                <p>⚠️ <strong>Attention :</strong> Nous ne pouvons accompagner que 3 nouveaux artisans par semaine pour garantir cette rapidité.</p>
            </div>
        </section>
    </main>

    <Footer />
</BaseLayout>

<script>
    // --- SIMPLE SMS TRACKING ---
    // On peut passer un paramètre ?s=nom_artisan pour savoir qui clique
    const urlParams = new URLSearchParams(window.location.search);
    const source = urlParams.get('s') || 'direct_sms';
    
    const waButton = document.querySelector('.btn-whatsapp-large');
    if (waButton) {
        waButton.addEventListener('click', () => {
            console.log(`Conversion WhatsApp depuis : ${source}`);
            // Logique de tracking locale (LocalStorage) pour tes stats
            let stats = JSON.parse(localStorage.getItem('express_artisan_stats') || '{}');
            stats[source] = (stats[source] || 0) + 1;
            localStorage.setItem('express_artisan_stats', JSON.stringify(stats));
        });
    }
</script>

<style>
    .simple-nav {
        padding: 20px;
        text-align: center;
        background: white;
        border-bottom: 1px solid #eee;
    }

    .sms-landing {
        max-width: 600px;
        margin: 0 auto;
        padding: 40px 20px;
    }

    .sms-hero {
        text-align: center;
        margin-bottom: 60px;
    }

    .badge-alert {
        display: inline-block;
        background: #fef2f2;
        color: #e11d48;
        padding: 6px 15px;
        border-radius: 99px;
        font-weight: 800;
        font-size: 0.8rem;
        margin-bottom: 20px;
        border: 1px solid #fecdd3;
    }

    h1 {
        font-size: 2.2rem;
        line-height: 1.2;
        margin-bottom: 20px;
    }

    h1 span {
        color: var(--primary);
    }

    .offer-highlight {
        display: flex;
        justify-content: center;
        align-items: center;
        gap: 20px;
        background: var(--darker);
        padding: 30px;
        border-radius: 20px;
        color: white;
        margin: 30px 0;
    }

    .oh-item {
        display: flex;
        flex-direction: column;
    }

    .price { font-size: 2.5rem; font-weight: 900; color: var(--primary-alt); }
    .bonus { font-size: 1.5rem; font-weight: 900; color: var(--secondary); }
    .label { font-size: 0.8rem; opacity: 0.7; }
    .oh-plus { font-size: 1.5rem; opacity: 0.3; }

    .btn-whatsapp-large {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        background: #25d366;
        color: white;
        padding: 20px;
        border-radius: 15px;
        text-decoration: none;
        font-weight: 800;
        font-size: 1.1rem;
        box-shadow: 0 10px 20px rgba(37, 211, 102, 0.2);
    }

    .sub-cta {
        margin-top: 10px;
        font-size: 0.85rem;
        color: var(--gray);
    }

    .sms-benefits {
        margin-bottom: 60px;
    }

    .sms-benefits h2 {
        font-size: 1.5rem;
        margin-bottom: 30px;
        text-align: center;
    }

    .benefit-row {
        display: flex;
        gap: 20px;
        margin-bottom: 25px;
        background: white;
        padding: 20px;
        border-radius: 15px;
        box-shadow: 0 5px 15px rgba(0,0,0,0.02);
    }

    .b-icon { font-size: 1.8rem; }
    .b-text strong { display: block; font-size: 1rem; }
    .b-text p { font-size: 0.9rem; color: var(--gray); }

    .urgency-card {
        background: #fffbeb;
        border: 1px solid #fef3c7;
        padding: 20px;
        border-radius: 15px;
        color: #92400e;
        font-size: 0.9rem;
        text-align: center;
    }
</style>
